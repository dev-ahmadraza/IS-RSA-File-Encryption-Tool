# IS-RSA-File-Encryption-Tool
This is my semester project of IS lab course. In this project, we use RSA algorithm to perform encryption on different files. First system generate the keys(public and private), then encrypt the file using public key and receiver decrypt it using private key. This is the short overview of my project.  
